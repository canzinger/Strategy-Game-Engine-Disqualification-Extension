plugins {
    id 'java'
    id 'idea'
    id "maven-publish"
    id 'com.github.johnrengelman.shadow' version '4.0.2'
    id "com.jfrog.bintray" version "1.8.4"
}

group 'at.ac.tuwien.ifs.sge'
version '0.0.0'

sourceCompatibility = 1.11

repositories {
    mavenCentral()
    jcenter()
}


dependencies {

    compile 'info.picocli:picocli:3.9.6'
    //   compile 'org.jgrapht:jgrapht-core:1.3.0'
    compile group: 'org.fusesource.jansi', name: 'jansi', version: '1.18'
    compile group: 'com.google.guava', name: 'guava', version: '28.0-jre'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

bintray {
    user = "${bintrayName}"
    key = "${bintrayKey}"
    publications = ['mavenPublication']
    pkg {
        repo = 'StrategyGameEngine'
        name = 'at.ac.tuwien.ifs.sge'
        version {
            name = '0.0.0'
            released = new Date()
        }
    }
}

jar {
    manifest {
        attributes 'Main-Class': 'dev.entze.sge.engine.cli.SgeCommand'
    }
}

shadowJar {
    classifier = 'exe'
}

task sourcesJar(type: Jar, dependsOn: classes) {
    classifier = 'sources'
    from sourceSets.main.allSource
}

javadoc {
    if (JavaVersion.current().isJava9Compatible()) {
        options.addBooleanOption('html5', true)
    }
}

task javadocJar(type: Jar, dependsOn: javadoc) {
    classifier = 'javadoc'
    from javadoc.destinationDir
}

artifacts {
    archives sourcesJar
    archives javadocJar
}

publishing {
    publications {
        mavenPublication(MavenPublication) {
            from components.java
            artifact sourcesJar
            artifact javadocJar
            pom {
                name = "sge"
                description = "The core engine library, needed to implement games and agents."
                licenses {
                    license {
                        name = "GNU Affero General Public License version 3"
                        url = "https://opensource.org/licenses/agpl-v3.html"
                        distribution = "repo"
                    }
                }
                developers {
                    developer {
                        id = "entze"
                        name = "Lukas Grassauer"
                        email = "entze@grassauer.eu"
                    }
                }

                scm {
                    url = "https://gitlab.com/Entze/strategy-game-engine"
                    connection = "https://gitlab.com/Entze/strategy-game-engine.git"
                    developerConnection = "git@gitlab.com:Entze/strategy-game-engine.git"
                }
            }
            repositories {
                maven {
                    url = "https://jcenter.bintray.com/at/ac/tuwien/ifs/sge/sge/"
                }
            }
        }
    }
}
